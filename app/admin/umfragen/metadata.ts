import type { Metadata } from "next";
import { appName } from "@/lib/site-config";

export const metadata: Metadata = {
  title: `Umfragen verwalten | Admin | ${appName}`,
  description: "Umfragen verwalten",
};
