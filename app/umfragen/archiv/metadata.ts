import type { Metadata } from "next";
import { appName } from "@/lib/site-config";

export const metadata: Metadata = {
  title: `Umfragen-Archiv | ${appName}`,
  description: `Ältere Umfragen der ${appName}`,
};
